<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="LechYTDLP.Components.SelectFormat"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:classes="using:LechYTDLP.Classes"
             xmlns:local="using:LechYTDLP.Components"
             xmlns:controls="using:CommunityToolkit.WinUI.Controls"
             xmlns:ui="using:CommunityToolkit.WinUI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Unloaded="Dialog_Unloaded"
             mc:Ignorable="d">

    <UserControl.Resources>
        <!--  These styles can be referenced to create a consistent SettingsPage layout  -->
        <!--  Spacing between cards  -->
        <x:Double x:Key="SettingsCardSpacing">4</x:Double>
    </UserControl.Resources>


    <StackPanel Orientation="Vertical"
                Spacing="10"
                MaxWidth="{Binding ActualWidth, ElementName=VideoInfoGrid}">

        <StackPanel.ChildrenTransitions>
            <EntranceThemeTransition FromVerticalOffset="50" />
            <RepositionThemeTransition IsStaggeringEnabled="False" />
        </StackPanel.ChildrenTransitions>

        <StackPanel Spacing="10"
                    Margin="0,0,0,6">

            <StackPanel.ChildrenTransitions>
                <EntranceThemeTransition FromVerticalOffset="50" />
                <RepositionThemeTransition IsStaggeringEnabled="False" />
            </StackPanel.ChildrenTransitions>

            <!--Video Info-->
            <Grid ColumnSpacing="12"
                  x:Name="VideoInfoGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <!-- Resim -->
                    <ColumnDefinition Width="*" />
                    <!-- Metinler (Kalan yer) -->
                </Grid.ColumnDefinitions>

                <!-- Resim Alanı: Sabit Boyutlandırma -->
                <Border CornerRadius="{StaticResource ControlCornerRadius}"
                        x:Name="ThumbnailImageBorder"
                        Height="100"
                        Width="177"
                        MaxWidth="180"
                        Loaded="ThumbnailImageBorder_Loaded"
                        MinWidth="170">
                    <Border.Background>
                        <LinearGradientBrush x:Name="ShimmerBrush"
                                             StartPoint="0,0"
                                             EndPoint="1,0"
                                             MappingMode="RelativeToBoundingBox">
                            <GradientStop Color="#22FFFFFF"
                                          Offset="0" />
                            <GradientStop Color="#11FFFFFF"
                                          Offset="0.5" />
                            <GradientStop Color="#22FFFFFF"
                                          Offset="1" />
                        </LinearGradientBrush>
                    </Border.Background>

                    <Image x:Name="ThumbnailImage"
                           Height="100"
                           Width="177"
                           Stretch="Uniform"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           MaxWidth="180"
                           ImageOpened="ThumbnailImage_ImageOpened"
                           MinWidth="100" />
                </Border>

                <!-- Metin Alanı -->
                <Grid Grid.Column="1"
                      RowSpacing="4"
                      VerticalAlignment="Top">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Title -->
                    <TextBlock Grid.Row="0"
                               x:Name="VideoTitle"
                               Style="{ThemeResource SubtitleTextBlockStyle}"
                               TextTrimming="CharacterEllipsis"
                               MaxLines="2"
                               TextWrapping="Wrap" />

                    <!-- Uploader -->
                    <RichTextBlock Grid.Row="1"
                                   x:Name="VideoUploaderAndExtractor"
                                   Style="{ThemeResource BodyRichTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   MaxLines="1"
                                   TextWrapping="NoWrap" />

                    <!--Video Alt Info (Saving to bla bla)-->
                    <TextBlock Grid.Row="2"
                               x:Name="VideoAltInfo"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextTrimming="CharacterEllipsis"
                               MaxLines="2"
                               TextWrapping="Wrap" />
                    <!--<labs:MarqueeText Behavior="Looping"
                          Direction="Left"
                          Grid.Row="2"
                          FontSize="12"
                          x:Name="VideoAltInfo"
                          Style="{ThemeResource CaptionTextBlockStyle}"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                          RepeatBehavior="Forever" />-->
                </Grid>
            </Grid>

            <!--Presets-->
            <Grid ColumnSpacing="12"
                  x:Name="PresetsGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0"
                          x:Name="PresetSelect"
                          x:Uid="Preset"
                          DisplayMemberPath="Text"
                          PlaceholderText="Select"
                          SelectionChanged="SelectionChanged" />
                <TextBlock Grid.Column="1"
                           x:Name="PresetInfo"
                           VerticalAlignment="Bottom"
                           TextWrapping="Wrap"
                           Style="{ThemeResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </Grid>
        </StackPanel>

        <Border BorderThickness="1"
                BorderBrush="{StaticResource DividerStrokeColorDefaultBrush}"
                Background="#FFFFFFFF"
                x:Name="FormatBorder" />

        <!--<controls:Segmented x:Name="segmentedControl"
                            HorizontalAlignment="Stretch"
                            SelectedIndex="0">
            <controls:SegmentedItem Content="Square"
                                    Icon="{ui:FontIcon Glyph=&#xE739;}"
                                    Tag="square" />
            <controls:SegmentedItem Content="Circle"
                                    Icon="{ui:FontIcon Glyph=&#xEA3A;}"
                                    Tag="circle" />
            <controls:SegmentedItem Content="Rectangle"
                                    Icon="{ui:FontIcon Glyph=&#xE7FB;}"
                                    Tag="rect" />
        </controls:Segmented>-->

        <!--Video-->
        <Grid ColumnSpacing="12"
              x:Name="VideoFormatGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0"
                      x:Name="ResolutionSelect"
                      x:Uid="ResolutionSelect"
                      DisplayMemberPath="Text"
                      PlaceholderText="Select"
                      SelectionChanged="SelectionChanged" />

            <ComboBox Grid.Column="1"
                      x:Name="CodecSelect"
                      x:Uid="Codec"
                      DisplayMemberPath="Text"
                      Opacity="0"
                      PlaceholderText="Select"
                      SelectionChanged="SelectionChanged" />

            <TextBlock Grid.Column="2"
                       x:Name="VideoInfo"
                       VerticalAlignment="Bottom"
                       TextWrapping="Wrap"
                       Style="{ThemeResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </Grid>
        <!--Audio-->
        <Grid ColumnSpacing="12"
              x:Name="AudioFormatGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0"
                      x:Name="AudioSelect"
                      x:Uid="AudioSelect"
                      DisplayMemberPath="Text"
                      PlaceholderText="Select"
                      SelectionChanged="SelectionChanged" />

            <TextBlock Grid.Column="1"
                       x:Name="AudioInfo"
                       VerticalAlignment="Bottom"
                       TextWrapping="Wrap"
                       Style="{ThemeResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </Grid>

        <!--Playlist video selection-->
        <Grid ColumnSpacing="12"
              x:Name="PlaylistVideoSelectionGrid"
              Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ListView Grid.Column="0"
                      x:Name="PlaylistVideoSelection"
                      MaxHeight="300"
                      MaxWidth="{Binding ActualWidth, ElementName=VideoInfoGrid}"
                      SelectionChanged="SelectionChanged"
                      SelectionMode="Multiple">

                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsStackPanel>
                            <ItemsStackPanel.ChildrenTransitions>
                                <RepositionThemeTransition IsStaggeringEnabled="False" />
                            </ItemsStackPanel.ChildrenTransitions>
                        </ItemsStackPanel>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="classes:PlaylistVideoInfo">
                        <Expander IsExpanded="False"
                                  ExpandDirection="Down"
                                  HorizontalAlignment="Stretch">
                            <Expander.Header>
                                <TextBlock Text="{x:Bind NumberedTitle}"
                                           TextTrimming="CharacterEllipsis" />
                            </Expander.Header>
                            <Expander.Content>
                                <Grid ColumnSpacing="12" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                    <Grid.ColumnDefinitions>
                                        <!-- Resim -->
                                        <ColumnDefinition Width="Auto" />
                                        <!-- Metinler (Kalan yer) -->
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <!--<Border CornerRadius="{StaticResource ControlCornerRadius}"
                                            Height="50"
                                            Width="89"
                                            MaxWidth="80"
                                            MinWidth="85"
                                            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                            Grid.Column="0">
                                        <Image x:Name="PlaylistVideoThumbnail"
                                               Height="50"
                                               Width="89"
                                               Stretch="UniformToFill"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Source="{x:Bind BestThumbnailUrl}"
                                               MaxWidth="90"
                                               MinWidth="50" />
                                    </Border>-->

                                    <Grid Padding="0,6"
                                          ColumnSpacing="4"
                                          Grid.Column="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <ComboBox DisplayMemberPath="Text"
                                                  Grid.Column="1"
                                                  SelectedItem="{Binding SelectedPreset, Mode=TwoWay}"
                                                  ItemsSource="{Binding Presets}"
                                                  x:Name="PresetComboBox"
                                                  IsEnabled="False"
                                                  PlaceholderText="Select" />
                                                  <!--IsEnabled="{Binding IsSelectEnabled}"-->
                                    </Grid>
                                    <!--<StackPanel Padding="0,6" Orientation="Vertical" Spacing="6">
</StackPanel>-->
                                </Grid>
                            </Expander.Content>
                        </Expander>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Border BorderThickness="1"
                BorderBrush="{StaticResource DividerStrokeColorDefaultBrush}"
                Background="#FFFFFFFF"
                x:Name="PlaylistOptionsBorder"
                Visibility="Collapsed" />


        <!--Video selection options grid-->
        <Grid ColumnSpacing="12"
              x:Name="VideoSelectionOptionsGrid"
              Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    x:Name="SelectAllButton"
                    Click="Click" />

            <ComboBox Grid.Column="1"
                      x:Name="ChangeAllPresetsComboBox"
                      DisplayMemberPath="Text"
                      PlaceholderText="Preset for all videos"
                      SelectionChanged="SelectionChanged" />
        </Grid>
    </StackPanel>
</UserControl>
